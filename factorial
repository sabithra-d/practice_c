#include<stdio.h>
int main()
{
    int n,*p,f=1,i;
    printf("\nEnter a number");
    scanf("%d",&n);
    p=&f;
    for(i=1;i<=n;i++)
    {
        *p = (*p) * i;
    }
    printf("\nFactorial = %d",f);
    return 0;
}
