#include<stdio.h>
void main()
{
    int n,i;
    
    printf("Enter n:");
    scanf("%d",&n);
    printf("Odd divisors are:");
    for(i=1;i<=n;i++)
    {
        if(n % i == 0 && i % 2 != 0)
        {
            printf("\n%d ",i);
            
        }
    }
}
