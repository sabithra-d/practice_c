#include<stdio.h>
void reverse(int *);
int main()
{
    int a[10],i;
    printf("\nenter 10 array elements");
    for(i=0;i<10;i++)
        scanf("%d",&a[i]);
    reverse(a);
    return 0;
}
void reverse(int *p)
{
    int i;
    for(i=9;i>=0;i--)
    {
    printf("\n%d",*(p+i));
    }
}
